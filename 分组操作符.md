## 分组操作符()

> 分组操作符由一对括号组成，分组操作符的内部只能包含表达式

在使用eval对JSON进行执行的时候，JSON字符串通常被包含在一个圆括号里：eval('(' + json + ')')，这样做的原因就是因为分组操作符可以让解析器强制将JSON的花括号解析成::表达式而不是代码块。

```js
try {
  { "x": 5 }; // "{" 和 "}" 做解析成代码块
} catch(err) {
  // SyntaxError
}
({ "x": 5 }); // 分组操作符强制将"{" 和 "}"作为对象字面量来解析。
```
